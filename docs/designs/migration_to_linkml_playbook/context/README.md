# context/

Background material that doesn't belong in the procedure: design notes, references, deeper explanations of constraints, links to related discussions (issues, PRs, upstream tickets in `pydantic2linkml` / `linkml` / `dandi-cli` / `dandi-archive`).

Keep one topic per file; name files in kebab-case (e.g. `discriminated-unions.md`, `jsonschema-frontend-requirements.md`).
